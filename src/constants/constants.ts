export const PAGE_SIZE = 8;

export const LIGHT_MODE = "light",
	DARK_MODE = "dark",
	AUTO_MODE = "auto";
export const DEFAULT_THEME = AUTO_MODE;

// Show the back-to-top control after scrolling this percentage of the viewport.
export const BACK_TO_TOP_SHOW_THRESHOLD_VH = 35;

// Page width: rem
export const PAGE_WIDTH = 88;
